#!/usr/bin/env bash
# Runs after the image is built but before the session starts
#echo "Fetching large artefacts..."
#wget -q https://example.com/evaluation-data.zip -O /tmp/eval.zip
#unzip -q /tmp/eval.zip -d ./your-eval-folder/
echo "Installing Jupyterlab..."
pip install jupyterlab
